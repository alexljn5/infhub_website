# ============================================================
# INFHUB Docker Environment Configuration
# ============================================================
# Copy this file to .env and update the values below.
#   cp .env.example .env
#
# IMPORTANT: Use strong, unique passwords in production!
# ============================================================

# MariaDB root password (used for healthcheck and admin access)
DB_ROOT_PASSWORD=change_me_to_a_strong_password

# MariaDB application user password (used by the PHP app)
DB_PASSWORD=change_me_to_a_strong_password

# The Lounge IRC client configuration
# (These are set automatically by docker-compose; override only if needed)
# LOUNGE_HOST=lounge
# LOUNGE_PORT=9000
